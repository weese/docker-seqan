# Run SeqAn Apps in Docker
Docker image consisting of all the nice SeqAn apps.

Build:

```
docker build -t davomat/docker-seqan .
```

Run:

```
docker run -ti davomat/docker-seqan
razers3 --help
```
